<%= form_for(@moniter) do |f| %>
  <% if @moniter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moniter.errors.count, "error") %> prohibited this moniter from being saved:</h2>
      <ul>
      <% @moniter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :screen, :class => "col-sm-2 col-sm-2 control-label"%>
    <div class="col-sm-10">
      <%= f.text_field :screen, :class =>"form-control" %> <br>
    </div>
  </div>
  <div class="actions">
    <%= f.submit 'Save', :class =>"btn btn-success btnnew btnalign" %>
  </div>
<% end %>

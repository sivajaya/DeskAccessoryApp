<%= form_for(@peripheral) do |f| %>
  <% if @peripheral.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@peripheral.errors.count, "error") %> prohibited this peripheral from being saved:</h2>

      <ul>
      <% @peripheral.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :name, :class => "col-sm-2 col-sm-2 control-label"%>
    <div class="col-sm-10">
      <%= f.text_field :name, :class =>"form-control" %> <br>
    </div>
  </div>
  <div class="actions">
    <%= f.submit 'Save', :class =>"btn btn-success btnnew btnalign" %>
  </div>
<% end %>
